import React from 'react';
import { HashLoader } from 'react-spinners';

const Loader = () => (
  <div className="loader">
    <HashLoader size={80} color="#1923C0" />
  </div>
);

export default Loader;
